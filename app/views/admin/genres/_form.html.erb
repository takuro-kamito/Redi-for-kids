<%= form_with model: genre, url: admin_genres_path, method: :post, local: false do |f| %>
<div class="mt-5">
  <strong>ジャンル名</strong>
  <span class="pl-5"><%= f.text_field :name, id: "genre_name" %></span>
  <span class="pl-3"><%= f.submit "新規登録", class: "btn btn-success" %></span>
</div>
<% end %>