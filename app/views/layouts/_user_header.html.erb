<header>
 <nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #C68E46;">
    <div class="container">
      <%= link_to(root_path, class: "navbar-brand") do %>
        <%= image_tag("logo-1.png", alt: "ロゴ", style: "width: 45px; height: auto;") %>
          <% end %>
           <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
             </button>
              <div class="collapse navbar-collapse" id="navbarNavDropdown">
               <ul class="navbar-nav ml-auto">
                <% if user_signed_in? %>
                 <div class="mt-3 mr-5" style="color: white;">
              ようこそ、<%= current_user.name %>さん！
            </div>
          <li>
            <%= link_to user_user_path(current_user.id), class: 'nav-link text-light' do %>
              <i class="fas fa-home"></i>My Page
            <% end %>
          </li>
          <li>
            <%=  link_to user_users_path, class: 'nav-link text-light' do %>
              <i class="fas fa-users"></i> User
            <% end %>
          </li>
          <li>
            <%=  link_to user_communities_path ,class: 'nav-link text-light' do %>
              <i class="fas fa-book-open"></i> Community
              <% end %>
          </li>
          <li>
            <%= link_to destroy_user_session_path, method: :delete, class: 'nav-link text-light' do %>
              <i class="fas fa-sign-out-alt"></i> Log out
            <% end %>
          </li>
        <% else %>
          <li>
            <%= link_to root_path, class: 'nav-link text-light' do %>
              <i class="fas fa-home"></i> Home
            <% end %>
          </li>
          <li>
            <%= link_to user_about_path, class: 'nav-link text-light' do %>
              <i class="fas fa-link"></i> About
            <% end %>
          </li>
          <li>
            <%= link_to new_user_registration_path, class: 'nav-link text-light' do %>
              <i class="fas fa-user-plus"></i> Sign up
            <% end %>
          </li>
          <li>
            <%= link_to user_session_path , class: 'nav-link text-light' do %>
              <i class="fas fa-sign-in-alt"></i> Log in
            <% end %>
          </li>
        <% end %>
        </ul>
      </div>
     </div>
    <div>
   <%= render 'user/searches/form' %>
  </div>
 </nav>
</header>
