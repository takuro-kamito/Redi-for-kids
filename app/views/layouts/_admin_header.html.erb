<header>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="/"><span>redi for kids</span></a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
        <div class="mt-3 mr-5" style="color: white;">
              管理人ページ
            </div>
          <li>
            <!--%=  link_to path, class: 'nav-link text-light' do %>ページ作成後記述-->
              <i class="fa-solid fa-users"></i> 会員一覧
            <!--% end %>-->
          </li>
          <li>
            <%=  link_to admin_communities_path ,class: 'nav-link text-light' do %>
              <i class="fa-solid fa-comments"></i></i> Community作成
            <% end %>
          </li>
          <li>
            <!--%= link_to admin_community_post_boards_path(@community), class: 'nav-link text-light' do %>-->
            <i class="fa-solid fa-file-pen"></i></i>Community編集
            <!--% end %>-->
          </li>
          <li>
            <%= link_to destroy_user_session_path, method: :delete, class: 'nav-link text-light' do %>
              <i class="fas fa-sign-out-alt"></i> Log out
            <% end %>
          </li>
        <% end %>
        </ul>
      </div>
    </div>
  </nav>
</header>
