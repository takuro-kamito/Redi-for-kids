<div class="container p-4">
  <div class="row p-4">
    <div class="col-md-3">
      <div class="row-mt-2">
        <%= render 'user/searches/genre_search' %>
      </div>
    </div>
    <h2>検索結果</h2>
    <div class="col-md-8">
    <h2 class="mb-4 text-center"><%= @genre.name %></h2>
    <div class="row">
     <% @communities.each do |community| %>
    <div class="col-4">
    <div class="aspect-ratio-box">
      <%= link_to user_community_path(community.id) do %>
        ここにリンクの内容（画像やテキストなど）を記述
      <% end %>
    </div>
    <div class="mt-3 text-left">
      <b><%= community.name %></b>
    </div>
  </div>
<% end %>
      </div>
    </div>
  </div>
</div>
