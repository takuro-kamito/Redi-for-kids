<div class="bg_pattern Rectangles"></div>
 <div class="container p-4">
  <div class="row">
    <div class="col-5">
      <div class="d-flex flex-column align-items-start">
        <div style="text-align: left">
          <%= image_tag "panda2.png", size: '550x600', class: 'pomuo' %>
          <div class="balloon3-left ml-4">
            <p><br>
              検索も使ってね😄<br>
              新しいミーティングを<br>
              作りたい時は<br>
              なんだパンダで検索。<br>
              直接DMしてねー👍<br>
            </p>
          </div>
        </div>
      </div>
    </div>
    <div class="text-center">
  <h2>Member</h2>
  <div class="mx-auto" style="background-color: rgba(252, 250, 247, 0.2); color: white; width: 140%">
  <table class="table">
    <thead>
      <tr>
        <th>icon</th>
        <th>name</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
      <% if user != current_user %>
        <tr>
          <td><%= image_tag user.get_profile_image(100, 100), class: "rounded-circle mx-auto d-block" %></td>
          <td><%= user.name %></td>
          <td>
            <%= link_to "My page", user_path(user.id), class: "btn btn-primary btn-cute"  %>
          </td>
         </tr>
         <% end %>
        <% end %>
       </tbody>
      </table>
     <div class="users_paginate">
    <%= paginate @users %>
   </div>
  </div>
 </div>
 <%= stylesheet_link_tag 'styles' %>
<style>
.users_paginate{
  width: 100%;
  margin: auto;
}

/* ページネーション全体 */
.pagination{
  text-align: center;
  display: flex;
  justify-content: center;
  padding: 60px;
}

/* ページネーションのspan */
.pagination span{
  text-align: center;
  width: 50px;
  font-size: 20px;
}

/* ページネーションのリンク文字（a） */
.pagination a{
  color: #818181;
  display: block;
  width: 100%;
  height: 100%;
  font-weight: bold;
  padding: 10px;
}

/* ページネーションの文字にカーソルを載せた時 */
.pagination a:hover{
  width: 50px;
  height: 50px;
  background-color: #84b8eb;
  color: #FFF;
  border-radius: 100%;
  padding: 10px;
}

/* 現在のページの数字についての表記 */
.current{
  font-weight: bold;
  color: #84b8eb;
  padding: 10px;
}
.balloon3-left {
  left: -170px;
  position: absolute;
  display: inline-block;
  margin: 1.0em 0 1.0em 10px;
  padding: 0 80px;
  width: 330px;
  height: 200px;
  line-height: 105px
  text-align: center;
  color: #FFF;
  font-size: 16px;
  font-weight: bold;
  background: #ffcc75;
  border-radius: 50%;
  box-sizing: border-box;
}

.balloon3-left:before {
  content: "";
  position: absolute;
  top: 50%;
  right: -26px;
  margin-top: -20px;
  border: 15px solid transparent;
  border-left: 15px solid #ffcc75;
  z-index: 0;
}
table {
  border-color: #000001; /* 黒色の線に変更 */
}
</style>