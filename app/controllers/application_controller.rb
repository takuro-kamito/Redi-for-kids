class ApplicationController < ActionController::Base
 
#   before_action :check_status
  
#   private

#   def check_status
#     if current_user && current_user.status == "制限中" # statusが制限中の場合
#       redirect_to new_user_session_path # ログインページへリダイレクト
#     end
#   end
end
